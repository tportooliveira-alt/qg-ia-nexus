{
  "name": "qg-ia-nexus-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "npm start --workspace=apps/api",
    "dev": "npm run dev --workspace=apps/api",
    "test": "npm test --workspace=apps/api",
    "build": "turbo run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@tailwindcss/oxide": "^4.2.2",
    "dotenv": "^17.3.1",
    "ssh2": "^1.17.0"
  }
}
